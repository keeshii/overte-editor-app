import { ScrollWhenDirective } from './scroll-when.directive';

describe('ScrollWhenDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollWhenDirective();
    expect(directive).toBeTruthy();
  });
});
